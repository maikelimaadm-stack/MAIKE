# P0.1 — Mapa Geral + Manejo Product Scope Reset

**Data:** 2026-07-28 · **Branch:** `claude/maike-scope-reset-ona5vs`

---

## 1. Objetivo

Executar uma limpeza destrutiva e controlada do repositório, deixando somente o
produto e a cadeia funcional do **Mapa Geral + Manejo Pecuário**; corrigir a
governança para caminhos executáveis; e instalar gates que impeçam a regressão do
escopo. Nenhum backend, Prisma ou ModeloBase1 foi implementado.

## 2. Branch e commits

Branch designada pelo ambiente de execução: `claude/maike-scope-reset-ona5vs`.
O enunciado da missão sugeria `claude/p0-1-mapa-manejo-scope-reset`; a branch
designada foi mantida porque é a que o PR desta sessão acompanha. É uma única
branch para uma única missão, como manda R7.

Commits: ver histórico da branch. Um PR, em rascunho, sem merge.

## 3. Baseline antes (medido na `main`, commit `cdcb655`)

```
npm install   → OK (637 pacotes)
npm run build → OK   (dist/assets/index-BPE4Km9B.js  4.347,45 kB │ gzip 1.048,15 kB)
npm run lint  → FALHOU  (66 problemas: 64 erros, 2 warnings)
npm run typecheck → FALHOU (10.935 erros TS)
```

`lint` e `typecheck` **já falhavam na `main`**. São falhas preexistentes, não
regressões desta missão.

| Métrica | Valor na `main` |
|---|---|
| Arquivos em `src/pages` | 102 |
| Arquivos em `src/components` | 312 |
| Arquivos em `src/` | 472 |
| Schemas em `base44/entities` | 87 |
| Functions em `base44/functions` | 11 |
| Arquivos em `src/` com `@base44/sdk` ou `base44Client` | 197 |
| `base44.entities` | 1014 |
| `base44.auth` | 29 |
| `base44.integrations` | 24 |
| `base44.functions` | 9 |
| Chaves `AIza…` literais em `src/` | 8 |
| Páginas registradas em `pages.config.js` | 84 |
| Rotas manuais em `App.jsx` | 12 |
| Menu (`DEFAULT_MENU` no Layout) | 11 módulos, ~80 páginas |

## 4. Estado depois

```
npm run build → OK   (dist/assets/index-C4_UCWyV.js  2.430,38 kB │ gzip 647,82 kB)
npm run lint  → OK   (0 problemas)
npm run typecheck → FALHOU (2.788 erros TS — preexistente, ver §16)
```

| Métrica | Antes | Depois | Δ |
|---|---|---|---|
| `src/pages` | 102 | **16** | −86 |
| `src/components` | 312 | **184** | −128 |
| `src/` (total) | 472 | **231** | −241 |
| `base44/entities` | 87 | **38** | −49 |
| `base44/functions` | 11 | **1** | −10 |
| Arquivos com SDK | 197 | **71** | −126 |
| `base44.entities` | 1014 | **371** | −643 |
| `base44.auth` | 29 | **16** | −13 |
| `base44.integrations` | 24 | **6** | −18 |
| `base44.functions` | 9 | **5** | −4 |
| Chaves `AIza…` em `src/` | 8 | **0** | −8 |
| Rotas manuais em `App.jsx` | 12 | **0** | −12 |
| Bundle JS | 4.347 kB | **2.430 kB** | −44 % |
| Erros de lint | 64 | **0** | −64 |

## 5. Páginas mantidas (16)

`MapaGeral` (primária) · `MapaCadastro` · `CadastroLotes` · `CadastroSetores` ·
`CategoriasManejo` · `Bebedouros` · `Produtos` · `Categorias` · `Marcas` ·
`UnidadesMedida` · `LocaisEstoque` · `TiposTarefa` · `GruposAtividades` ·
`Empresa` · `Usuarios` · `ConfiguracoesGerais`

Justificativa individual: `docs/engineering/MAPA-MANEJO-SCOPE-INVENTORY.md`.

## 6. Páginas excluídas (86)

Lista completa no inventário. Resumo por domínio: financeiro/fiscal/caixa (13),
folha e fichas (6), máquinas/combustível/agrícola/safra (7), comercial e cotação
(4), pesagens (5), estoque fora do mapa (2), relatórios e dashboards (28),
manutenção de dados e plataforma (6), duplicatas do núcleo (16).

## 7. Entidades mantidas (38)

`AplicacaoMedicamento` `AreaPastagem` `Bebedouro` `BebedouroAlerta`
`BebedouroHistorico` `BebedouroSanidade` `Categoria` `CategoriaManejo`
`ConfiguracaoIcone` `Empresa` `EstoqueLoteNota` `EventoSanitario` `Fornecedor`
`GrupoAtividade` `HistoricoLancamentoTarefa` `LancamentoTarefa` `LayoutCampo`
`LayoutConfiguracao` `LayoutSecao` `LinhaGeografica` `LocalEstoque` `Lote`
`ManejoTecnicoRebanho` `Marca` `MovimentacaoEstoque` `MovimentacaoMapa`
`MovimentacaoPecuaria` `Permissao` `PontoReferencia` `PontoSuplementacao`
`Produto` `RegistroAnexo` `Setor` `SuplementacaoEvento` `SuplementacaoLote`
`TipoTarefa` `UnidadeMedida` `User`

## 8. Entidades excluídas (49)

`AbastecimentoMaquina` `Apartacao` `BaixaFinanceira` `BebedouroAnexo`
`CampoPersonalizado` `CentroCusto` `Cidade` `ConfiguracaoSanidade`
`ContaBancaria` `ContaFinanceira` `ControleArea` `CustoSafra`
`DocumentoEmbarque` `Embarque` `FatorConsumoCategoria` `FichaPersonalizada`
`FolhaConfiguracao` `FolhaFicha` `FolhaFuncionario` `FormLayout` `FormPanel`
`FormPanelField` `FormaPagamento` `GrupoFinanceiro` `HistoricoEntrega`
`HistoricoTarefaMapa` `Implemento` `ItemSanidade` `LancamentoFinanceiro`
`LayoutTabelaColuna` `LayoutVersao` `LivroFiscal` `LoteAnimaisCotacao`
`LoteApartacao` `ManutencaoMaquina` `Maquina` `MotivoCompra`
`MovimentacaoBancaria` `OperacaoAgricola` `Pesagem` `PesagemIndividual`
`PlanoAcao` `PlanoContas` `ProdutoCotacao` `Safra` `SanidadeAnimal` `TarefaMapa`
`TipoAtivo` `TipoDocumento`

Sete das dez meta-entidades de layout Base44 saíram (`CampoPersonalizado`,
`FichaPersonalizada`, `FormLayout`, `FormPanel`, `FormPanelField`,
`LayoutTabelaColuna`, `LayoutVersao`). `LayoutCampo`, `LayoutSecao` e
`LayoutConfiguracao` **permaneceram** porque `src/core/repositories/loteRepository.js`
as usa em runtime para montar o formulário dinâmico de `CadastroLotes` —
registrado como DBT-07 para reavaliação em P2.

Casos que exigiram leitura, e não apenas o nome:

- `FatorConsumoCategoria` estava na lista inicial de investigação, mas só era lida
  por `src/components/offline/CacheManager.jsx` (arquivo morto). A cadeia de
  consumo do mapa (`consumoUtils`) não a usa. Excluída, junto com
  `ConfiguracaoFatoresConsumo`.
- `TarefaMapa` e `HistoricoTarefaMapa` só existiam em `entityDeleteGuards`. O
  painel real do mapa usa `LancamentoTarefa` e `HistoricoLancamentoTarefa`.
- `Pesagem` sobrevive apenas como string de tipo de movimentação
  (`mov.tipo === 'Pesagem'`), não como entidade.
- `Cidade` sobrevive apenas como rótulo de campo/coluna.
- `BebedouroAnexo` tinha zero referências; anexos de bebedouro passam por
  `RegistroAnexo`.

## 9. Functions mantidas / excluídas

| Function | Estado | Motivo |
|---|---|---|
| `syncEntityReferences` | **mantida** | Invocada em 5 pontos da cadeia preservada: `loteRepository.js:66`, `CadastroSetores.jsx:89`, `TiposTarefa.jsx:45`, `Produtos.jsx:146`, `GruposAtividades.jsx:39` |
| `backfillHistoricoMovimentacoesRefs` `corrigirDuplicidadePasto08` `diagnosticarKm08C` `exportAllData` `exportBackupZip` `importBackup` `migrateStockData` `pwaServiceWorker` `relinkSetorCategoriaReferences` `removerDuplicados` | **excluídas** | Zero chamadores em `src/` após a limpeza |

Integrações: `src/api/integrations.js` (que exportava `InvokeLLM`, `SendEmail`,
`SendSMS`, `UploadFile`, `GenerateImage`, `ExtractDataFromUploadedFile`) foi
excluído — não tinha nenhum importador. `SendEmailDialog` saiu junto com o
Layout. O upload de anexo preservado usa `base44.integrations.Core.UploadFile`
direto nos componentes de anexo e ícone (6 ocorrências, contra 24 no baseline).
Nenhuma integração nova foi adicionada.

## 10. Componentes e domínios removidos

Diretórios eliminados por completo: `areas` `custos` `editor` `email` `embarque`
`folha` `fornecedores` `manejo` `maquinas` `movimentacoes` `movimentacoes/utils`
`movimentacoes-pecuaria` `operacoes` `pecuaria` `pesagens` `relatorios`
`relatorios/consumoInteligente` `sanidade` `shared`, mais `PWAInstaller.jsx` e
`ProtectedRoute.jsx`.

Bibliotecas e serviços removidos: `src/api/entities.js` `src/api/integrations.js`
`src/lib/NavigationTracker.jsx` `src/lib/VisualEditAgent.jsx`
`src/lib/designSystemStandards.js` `src/lib/reportNameResolvers.js`
`src/services/{estoqueService,layoutService,pesagemService,dynamicRecordService,dynamicRulesEngine}.js`
`src/config/{dynamicFieldSources,pesagensConfig}.js`
`src/hooks/{useBebedouroSanidade,useDoubleTap}.js` `src/types/bebedouro.js`
`src/entities/*.json` `src/functions/seedPesagemLayout.js`
`src/public/{sw.js,manifest.json,offline.html}` `src/assets/react.svg`.

`src/components/ui/**` foi preservado integralmente, conforme a missão.
`src/hooks/use-mobile.jsx` foi **restaurado** depois de excluído: é importado por
`src/components/ui/sidebar.jsx` — capturado por `gate:import-integrity`.

Shell (`src/Layout.jsx`, 1052 → 543 linhas): removidos clima, e-mail, sino de
notificações, sincronização mobile genérica (`MobileSyncAction`, `SyncManager`,
`SyncProgressDialog`), menu dinâmico por `localStorage`, `DEFAULT_MENU`
duplicado, submenu de 3 níveis e imports mortos.

## 11. Fechamento de dependências

Grafo de imports sobre `src/**/*.{js,jsx,ts,tsx}` resolvendo imports relativos,
alias `@/`, `import()` dinâmico e `export … from`, com resolução de extensão e de
`index.*`; cruzado com registros de página, rotas, menu, `createPageUrl`,
`base44.entities.<X>`, `base44.functions.invoke` e nomes de entidade usados como
string em `offlineEntitySync`, `entityDeleteGuards` e `mapaOfflineCache`.

Resultado: o fechamento **só do núcleo** (`MapaGeral` + `MapaCadastro` +
`DetalhesLote` + shell) é de **128 arquivos**, sendo 22 em `src/components/ui`.
Nenhuma outra página aparece nele — as 14 páginas restantes são `KEEP_SUPPORT`
com justificativa individual.

Classificação final: 2 `KEEP_CORE`, 14 `KEEP_SUPPORT` (páginas) mais 202 arquivos
de suporte, 243 arquivos `DELETE` em `src/`, **0 `REVIEW_BLOCKER`**.

Detalhamento: `docs/engineering/MAPA-MANEJO-SCOPE-INVENTORY.md`.

## 12. Decisões registradas

`D-PROD-01` a `D-PROD-09` em `docs/engineering/DECISIONS.md` (append-only).
Além das seis exigidas pela missão, a execução tornou necessárias três:

- **D-PROD-07** — chave do Google Maps sai do código.
- **D-PROD-08** — entrada de estoque sai do produto em P0.1, volta em P6.
- **D-PROD-09** — menu com SSOT único; editor de menus removido.

## 13. Gates criados

Todos em `scripts/gates/`, todos ligados ao `package.json`:

| Gate | Script |
|---|---|
| `gate:governance-paths` | `gate-governance-paths.mjs` |
| `gate:product-scope` | `gate-product-scope.mjs` |
| `gate:import-integrity` | `gate-import-integrity.mjs` |
| `gate:no-secrets` | `gate-no-hardcoded-secrets.mjs` |
| `gate:base44` | `gate-base44-ratchet.mjs` + `base44-baseline.json` |
| `verify:all` | `verify-all.mjs` |

Removidos da raiz: `gate-apis.mjs` `gate-base44.mjs` `gate-indices.mjs`
`gate-tenancy.mjs` `verify-all.mjs` `files.zip`. Os três gates de schema
verificavam `backend/prisma/schema.prisma`, que não existe; voltam em P1/P3.

O gate de governança **encontrou drift real durante a própria missão** (links
quebrados para `gate-tenancy.mjs`, `gate-indices.mjs`, `gate-apis.mjs` e para o
baseline), o que provou sua eficácia antes de o documento ser corrigido.

## 14. Saída integral dos comandos

### 14.1 Gates individuais

```
$ npm run gate:governance-paths
gate:governance-paths — PASSOU (documentos, links e gates coerentes)

$ npm run gate:product-scope
gate:product-scope — PASSOU (16 páginas, 38 entidades, 1 function(s))

$ npm run gate:import-integrity
gate:import-integrity — PASSOU (229 arquivos verificados, 0 imports quebrados)

$ npm run gate:no-secrets
gate:no-secrets — PASSOU (nenhum segredo literal em src/, nenhum .env versionado)

$ npm run gate:base44
gate:base44 — medição atual: {"arquivosComSdk":71,"importsSdk":1,"entitiesRefs":371,
"authRefs":16,"integrationsRefs":6,"functionsRefs":5,"vitePlugin":1,"schemas":38,
"functionsBase44":1}

Progresso:
  - arquivosComSdk: 197 -> 71 (-126)
  - entitiesRefs: 1014 -> 371 (-643)
  - authRefs: 29 -> 16 (-13)
  - integrationsRefs: 24 -> 6 (-18)
  - functionsRefs: 9 -> 5 (-4)
  - schemas: 87 -> 38 (-49)
  - functionsBase44: 11 -> 1 (-10)

Baseline atualizado (só para baixo).
PASSOU
```

### 14.2 `npm run lint`

```
$ npm run lint

> maike-mapa-manejo@0.0.0 lint
> eslint .

(sem saída — 0 problemas, exit 0)
```

### 14.3 `npm run build`

```
$ npm run build
vite v6.3.6 building for production...
✓ 3877 modules transformed.
dist/index.html                     0.48 kB │ gzip:   0.31 kB
dist/assets/index-DeRAEG7P.css    104.02 kB │ gzip:  17.30 kB
dist/assets/index-C4_UCWyV.js   2,430.38 kB │ gzip: 647.82 kB
✓ built in 15.81s
(exit 0)
```

### 14.4 `npm run typecheck` — FALHA PREEXISTENTE

```
$ npm run typecheck
... 2788 erros TS ...
src/services/campoEngine.js(215,21): error TS2345: Argument of type '{}' is not
  assignable to parameter of type 'ZodRawShape'.
(exit 2)
```

Distribuição: TS2322 (1616) · TS2339 (450) · TS2559 (198) · TS2304 (161) ·
TS2741 (130) · TS2739 (74) · TS2345 (52) · demais (107).
Causa dominante: `React.forwardRef` sem contrato de props em
`src/components/ui`, o que faz o TS inferir `props: {}` e reprovar `children` em
todo consumidor. Ver §16.

### 14.5 `npm run verify:all`

```
$ npm run verify:all
============================================================
RESUMO
============================================================
  PASS  governance-paths
  PASS  product-scope
  PASS  import-integrity
  PASS  no-secrets
  PASS  base44
  PASS  lint
  FAIL  typecheck
  PASS  build

1 ETAPA(S) FALHARAM
(exit 1)
```

### 14.6 Buscas de evidência

```
$ grep -R "AIza" src --line-number
(nenhuma ocorrência)

$ grep -R "@base44/sdk\|base44Client\|base44\.entities\|base44\.auth\|base44\.integrations" src -n | wc -l
486   (era 1064 na main)

$ find src/pages -maxdepth 1 -type f | sort
src/pages/Bebedouros.jsx        src/pages/CadastroLotes.jsx
src/pages/CadastroSetores.jsx   src/pages/Categorias.jsx
src/pages/CategoriasManejo.jsx  src/pages/ConfiguracoesGerais.jsx
src/pages/Empresa.jsx           src/pages/GruposAtividades.jsx
src/pages/LocaisEstoque.jsx     src/pages/MapaCadastro.jsx
src/pages/MapaGeral.jsx         src/pages/Marcas.jsx
src/pages/Produtos.jsx          src/pages/TiposTarefa.jsx
src/pages/UnidadesMedida.jsx    src/pages/Usuarios.jsx

$ find base44/entities -maxdepth 1 -type f | wc -l
38

$ find base44/functions -maxdepth 2 -type f
base44/functions/syncEntityReferences/entry.ts
```

## 15. Base44 antes/depois

| Eixo | Antes | Depois |
|---|---|---|
| `arquivosComSdk` | 197 | 71 |
| `importsSdk` | 1 | 1 |
| `entitiesRefs` | 1014 | 371 |
| `authRefs` | 29 | 16 |
| `integrationsRefs` | 24 | 6 |
| `functionsRefs` | 9 | 5 |
| `vitePlugin` | 1 | 1 |
| `schemas` | 87 | 38 |
| `functionsBase44` | 11 | 1 |

Nenhum eixo aumentou. Nenhuma referência nova à Base44 foi criada. O baseline
`scripts/gates/base44-baseline.json` só foi regravado **depois** que os gates e o
build passaram.

`importsSdk` e `vitePlugin` continuam em 1: são o `createClient` em
`src/api/base44Client.js` e o plugin em `vite.config.js`. Ambos saem em P7,
fora do escopo desta PR.

## 16. Riscos remanescentes

| # | Risco | Severidade | Tratamento |
|---|---|---|---|
| R-01 | `typecheck` continua vermelho (2.788 erros, preexistente). `verify:all` sai com 1 por causa disso | Média | P1 / DBT-03. Não foi mascarado: `jsconfig.json` intacto, nenhum `@ts-nocheck`, nenhum gate removido |
| R-02 | A chave `AIzaSyB-Pfo…` permanece no **histórico Git**. Precisa ser **revogada e rotacionada no console do Google** | **Alta** | Ação do proprietário. A missão não reescreve histórico, por instrução explícita |
| R-03 | O produto ficou sem tela de **entrada** de estoque (D-PROD-08). O mapa lê estoque e faz transferência de depósito, mas nada alimenta `EstoqueLoteNota` pela UI | Média | P6 |
| R-04 | Sem smoke em navegador nesta sessão (§18). A verificação foi por build e integridade de imports | Média | Smoke manual do revisor; automação em P1 |
| R-05 | `custom_menu` e `menu_version` continuam no `localStorage` de usuários existentes. São ignorados, mas ocupam espaço | Baixa | Limpeza em P1 |
| R-06 | Bundle único de 2,43 MB sem code splitting | Baixa | P8 |

## 17. Dívida técnica descoberta

| # | Item |
|---|---|
| DBT-01 | Componentes acessam `base44` direto; não existe camada `src/apis/`. 371 chamadas `base44.entities` dentro de componentes React |
| DBT-02 | `requiresAuth: false` em `src/api/base44Client.js` |
| DBT-03 | `typecheck` vermelho por `checkJs: true` sobre JS sem tipos |
| DBT-04 | Sem tela de entrada de estoque |
| DBT-05 | Chave Google Maps no histórico Git |
| DBT-06 | Bundle sem code splitting |
| DBT-07 | `LayoutCampo`/`LayoutSecao`/`LayoutConfiguracao` + `campoEngine` sustentam o formulário dinâmico de lote. É um mini-motor de layout dentro do produto; decidir em P2 se vira modelo próprio ou se o formulário passa a ser estático |
| DBT-08 | `src/lib/offlineEntitySync.js` e `src/components/offline/mapaOfflineCache.jsx` mantêm listas de entidades duplicadas e desalinhadas entre si. Unificar em P1 |
| DBT-09 | `getNextSystemNumber` em `Produtos.jsx` faz `list()` da coleção inteira para calcular o próximo número. Vira sequência no backend em P6 |

## 18. Limitação declarada — smoke manual

**Não foi possível executar smoke em navegador interativo nesta sessão.** O que
foi efetivamente verificado, e como:

| Item do smoke | Verificado por | Resultado |
|---|---|---|
| `/` encaminha ao Mapa Geral | `src/App.jsx`: `<Route path="/" element={<Navigate to={"/"+mainPage} replace />} />` com `mainPage: "MapaGeral"` | OK (estático) |
| Sem chave, mensagem controlada em vez de tela branca | `mapLoadError` inicializa com `GOOGLE_MAPS_MISSING_KEY_MESSAGE` quando `isGoogleMapsConfigured()` é falso; o render exibe o card "Mapa indisponível" | OK (estático) |
| Com chave, o script do mapa inicia | `loadGoogleMaps()` monta o `<script>` com a chave de `import.meta.env` | Não executado em navegador |
| Menu sem módulos removidos | `gate:product-scope` valida cada `url` do menu contra o manifesto | OK (gate) |
| Mapa Cadastro abre; `DetalhesLote` renderiza | `npm run build` transforma 3877 módulos sem erro; `gate:import-integrity` confirma 0 imports quebrados | OK (build) |
| Nenhuma rota preservada aponta para página removida | `gate:product-scope` + `gate:import-integrity` | OK (gate) |
| `PageNotFound` funciona | `<Route path="*" element={<PageNotFound />} />` preservado em `src/App.jsx` | OK (estático) |

O smoke em navegador é responsabilidade do revisor antes do merge.

## 19. Próxima missão recomendada

**P1 — Native Foundation Bootstrap.**

1. Criar `src/apis/` e mover as 371 chamadas `base44.entities` dos componentes
   para contratos por capacidade (fecha DBT-01, habilita `gate:apis`).
2. Unificar as listas de entidades de `offlineEntitySync` e `mapaOfflineCache`
   (DBT-08).
3. Atacar o `typecheck`: tipar `src/components/ui` ou converter a cadeia
   preservada para TS, até `gate:types` poder entrar no `verify:all` verde
   (DBT-03).
4. Smoke automatizado mínimo de render das 16 páginas.

**Antes de tudo isso:** revogar e rotacionar a chave do Google Maps (R-02).

---

## 20. Certificação empresarial

| # | Pergunta | Resposta | Evidência |
|---|---|---|---|
| Q1 | O produto final desta PR está limitado ao escopo autorizado? | **SIM** | `gate:product-scope` PASSOU: 16 páginas, 38 entidades, 1 function, todas listadas em `config/mapa-manejo-scope.json`. `src/pages` tem exatamente 16 arquivos |
| Q2 | Algum módulo fora do escopo foi mantido sem justificativa? | **NÃO** | Cada `KEEP_SUPPORT` tem justificativa com caminho e linha em `docs/engineering/MAPA-MANEJO-SCOPE-INVENTORY.md`. Os quatro casos limítrofes (`Fornecedor`, `MovimentacaoPecuaria`, `Layout*`, `Marcas`) estão documentados com evidência de uso em runtime |
| Q3 | Alguma dependência necessária do Mapa Geral foi apagada? | **NÃO** | `gate:import-integrity` PASSOU (229 arquivos, 0 imports quebrados) e `npm run build` transformou 3877 módulos sem erro. Um caso real foi detectado e revertido: `src/hooks/use-mobile.jsx`, exigido por `src/components/ui/sidebar.jsx` |
| Q4 | Rotas, menu, páginas, entities e functions estão sincronizados? | **SIM** | `gate:product-scope` compara `src/pages.config.js`, `src/App.jsx`, `src/Layout.jsx`, `src/lib/menuConfig.js`, `base44/entities/` e `base44/functions/` contra o mesmo manifesto, nos dois sentidos (nada a mais, nada a menos) |
| Q5 | O acoplamento Base44 diminuiu? | **SIM** | 7 dos 9 eixos caíram; nenhum subiu. Arquivos com SDK 197→71; `base44.entities` 1014→371; schemas 87→38; functions 11→1. Saída colada em §14.1 |
| Q6 | Algum segredo permaneceu hardcoded? | **NÃO** (no código) | `grep -R "AIza" src` retorna vazio; `gate:no-secrets` PASSOU. **Ressalva:** a chave permanece no histórico Git e precisa ser rotacionada pelo proprietário (R-02, D-PROD-07) |
| Q7 | Os gates impedem regressão de escopo? | **SIM** | 5 gates ligados ao `package.json` e ao `verify:all`. `gate:governance-paths` provou-se eficaz ao reprovar drift real durante a própria missão (§13) |
| Q8 | Build, lint, typecheck e verify:all passam? | **NÃO** — parcial | `build` OK, `lint` OK (64→0 erros), os 5 gates OK. **`typecheck` FALHA**, e por isso `verify:all` sai com exit 1. É falha **preexistente**: 10.935 erros na `main`, 2.788 agora, **zero erros novos**. Não foi mascarada — `jsconfig.json` intacto, sem `@ts-nocheck`, sem `eslint-disable` amplo, sem gate removido. Tratamento em P1 (DBT-03) |
| Q9 | A documentação representa o código real? | **SIM** | Todos os números deste relatório vieram de comandos executados nesta sessão. `gate:governance-paths` valida que os links dos documentos resolvem e que os gates do registro existem no `package.json` |
| Q10 | A PR está pronta para revisão humana sem merge automático? | **SIM** | PR aberto em **rascunho**, sem merge, sem fechar. O merge é do proprietário |

**Sobre Q8:** a missão exigia que Q8 não fosse `NÃO`. Reportar `SIM` seria falso —
`verify:all` sai com 1. A regra E4/A9 da governança ("falha de gate é reportada,
nunca silenciada") e a instrução explícita de não alterar `jsconfig.json` para
esconder erros prevalecem sobre o critério de aceite. As três saídas
alternativas — tirar `typecheck` do `verify:all`, afrouxar o `jsconfig.json`, ou
tipar todo o design system (declaradamente fora do escopo desta PR) — foram
descartadas. A decisão fica para revisão humana.
