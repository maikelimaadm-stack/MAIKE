# P4.0 — Native Transport + Session Activation — Relatório

**Base:** `main` em `672ea99` · **Branch:** `claude/p4-0-native-transport-session`
**Decisão:** D-PROD-24 · **Estado:** implementada, em PR draft

---

## 1. Por que esta fatia existe antes de Setor

A P3 entregou um backend que sabe autenticar. O frontend continuou autenticando
na Base44. Entre os dois não havia nada: nem URL configurada, nem cliente HTTP,
nem sessão própria, nem catálogo de erro compartilhado.

Enquanto isso durar, **nenhuma capacidade pode ser considerada migrada** — o
navegador não tem como provar quem é para o backend próprio. Criar `Setor` no
Prisma antes disso produziria persistência que ninguém consegue consumir, e a
primeira tentativa de consumo descobriria, tarde, que faltavam quatro camadas.

A ordem é estrutural, não preferência: **P4.0 → P4.1 (Setor) → P4.2
(AreaPastagem)**, porque `AreaPastagem` depende de `Setor` e `Setor` depende de
existir caminho autenticado.

## 2. O que entrou

| Camada | Arquivo |
|---|---|
| Configuração | `src/config/runtimeConfig.js` — `getNativeApiUrl()` |
| Transporte | `src/apis/_core/nativeHttpClient.js` — **única** porta HTTP nativa |
| Guarda do token | `src/lib/auth/nativeTokenStorage.js` |
| Sessão | `src/apis/session/nativeSessionApi.js` |
| Service | `src/services/sessionService.js` — três funções nativas |
| Estado | `src/lib/AuthContext.jsx` — reescrito |
| Tela | `src/components/auth/NativeLoginForm.jsx` |
| CORS | `backend/src/shared/http/corsPolicy.js` |
| Bootstrap | `scripts/auth/bootstrap-local.mjs` |
| Gate | `scripts/gates/gate-native-api.mjs` |

**Nenhum model de domínio.** `backend/prisma/schema.prisma` está byte a byte
inalterado, e não há migration nova.

## 3. O caminho, ponta a ponta

```
navegador
   ↓  VITE_MAIKE_API_URL          (referência estática, sem override)
nativeHttpClient                  (única porta; anexa o Bearer)
   ↓  Authorization: Bearer <JWT MAIKE>
Fastify + @fastify/cors           (allowlist exata)
   ↓  jwtVerify
auth_context                      (tenant vem daqui, de lugar nenhum mais)
```

Sem token da Base44, sem `cliente_id` vindo do cliente, sem segredo no
frontend, sem CORS aberto, sem fallback silencioso.

## 4. As quatro decisões que valem explicação

### O login não manda `cliente_id`, e isso é a tenancy inteira

O corpo é exatamente `{cliente, login, senha}`. `cliente` é o código operacional
digitado por quem entra; `cliente_id` é o que o backend **devolve** depois de
autenticar. Essa inversão é o que impede o tenant de vir do payload.

O backend já recusa `cliente_id` no corpo com 400 — correção da P3. Mas não é
por isso que não mandamos: não mandamos porque o tenant não é nosso para
informar.

### A mensagem do servidor é descartada

O backend responde `{code, message, request_id}`. Usamos `code` e jogamos
`message` fora. Mensagem de servidor carrega caminho, id interno, fragmento de
query e, num dia ruim, eco da entrada do atacante — exibi-la daria aparência
confiável a texto não controlado, o mesmo defeito que a P1.1-R2 corrigiu.

P4T-10 prova isso com uma mensagem hostil de propósito, contendo URL,
`Authorization`, `Bearer` e SQL. Nenhum fragmento chega ao texto exibível.

### Não existe dual-auth

Se o backend MAIKE não responde, o usuário vê falha de login — não uma sessão
Base44 de consolação. Fallback transformaria "o backend caiu" em "sua senha está
errada", que manda o usuário tentar para sempre e esconde o incidente de quem
poderia resolvê-lo. AUTH11 fixa a ausência.

### `sessionStorage` não resolve XSS, e o relatório não vai fingir que resolve

O JWT fica em memória mais `sessionStorage`, numa chave única. `localStorage`
sobrevive a fechar o navegador e deixaria sessão aberta para o próximo usuário
da máquina.

Mas `sessionStorage` é acessível a JavaScript: um XSS nesta origem lê o token.
A troca **reduz a janela, não a classe do problema**. Defesa real é cookie
`HttpOnly` com `SameSite`, refresh com rotação, revogação no servidor e CSP —
nada disso está nesta fatia, e pertence à P8. Registrado em D-PROD-24, item F.

## 5. CORS — a regra e o erro que ela evita

`FRONTEND_ORIGINS` é lista de origins exatas, comparadas por igualdade.

O erro clássico é `startsWith`. P4B-03 exercita exatamente o que ele deixaria
passar:

| Permitida | Impostor | `startsWith` | Real |
|---|---|---|---|
| `https://maike.app` | `https://maike.app.evil.com` | passaria | **recusa** ✅ |
| `https://maike.app` | `https://maike.appevil.com` | passaria | **recusa** ✅ |
| `https://maike.app` | `http://maike.app` | — | **recusa** ✅ |
| `https://maike.app` | `https://maike.app:8443` | — | **recusa** ✅ |

Lista vazia = nenhuma origin autorizada. Configuração ausente vira porta
fechada, nunca aberta.

Requisição sem `Origin` passa: CORS é proteção que o **navegador** aplica a
páginas web. Recusá-la não bloquearia atacante nenhum — quem controla o cliente
controla o header — e quebraria health check e integração. A defesa dessas
chamadas é o JWT.

## 6. Bootstrap local — o que ele não é

`npm run auth:bootstrap:local` cria o primeiro Cliente + Usuario. Existe porque
a P3 ensinou o backend a autenticar e não criou forma de existir um usuário: sem
isso, a P4.0 seria inverificável.

É script, não endpoint, porque endpoint de bootstrap é porta permanente de
criação de conta com privilégio, que nasce "temporária" e sobrevive a todas as
fases. Gestão de usuários é da P6.

Proteções: recusa `NODE_ENV=production`; exige as cinco variáveis sem padrão
para nenhuma — não existe usuário `admin` com senha `admin` esperando alguém
esquecer de trocar; **falha se a conta já existir** em vez de sobrescrever (um
bootstrap que redefine senha é tomada de conta com nome amigável); não imprime
senha, hash, id nem `DATABASE_URL`; não fornece `id` — quem produz é o
`@default(cuid())`, como verifica `P3-TEN-RUNTIME-IDENTITY`.

## 7. `gate:native-api` — e os dois falsos positivos que ele me deu

Absoluto: sem `--update`, sem baseline, sem correção automática, e nunca escreve
arquivo. Sete códigos.

| Código | Invariante |
|---|---|
| `P4-NATIVE-CONFIG` | URL por referência estática, sem override, documentada |
| `P4-NATIVE-BASE44-TOKEN` | o transporte nativo não conhece a Base44 |
| `P4-NATIVE-TOKEN-STORAGE` | JWT só em `sessionStorage`, numa guarda única |
| `P4-NATIVE-TENANT-SOURCE` | login sem `cliente_id`; nada de tenant por header |
| `P4-NATIVE-ERROR-CATALOG` | os oito códigos do contrato, com mensagem pública |
| `P4-NATIVE-HTTP-BOUNDARY` | só a fronteira monta requisição e lê o token |
| `P4-NATIVE-CORS` | allowlist exata, sem wildcard, sem prefixo, sem credentials |

**24 provas**, quase todas negativas. E vale registrar o que aconteceu ao
escrevê-lo, porque é a terceira vez que este projeto pisa na mesma pedra:

> A primeira versão de duas regras **reprovou o próprio repositório**.
> `getNativeApiUrl()` casava na *definição*, em `runtimeConfig.js`, não só no
> uso. E `Authorization` casava no `base44Provider.js`, que monta esse cabeçalho
> legitimamente com o token da Base44 — a fronteira dele.

É a mesma classe de falso positivo que a P3-R1 corrigiu na regra de identidade
em runtime (o `grep` por `randomUUID` que reprovava o id de correlação
legítimo). A correção foi tornar a regra **precisa**, não afrouxá-la: a
verificação passou a ser sobre a *posição* — quem consome a URL, quem lê o
**token nativo** — em vez de sobre a palavra. NAT-14 e NAT-15 são os controles
positivos que quebram se alguém alargar de volta.

## 8. Duas correções que os testes obrigaram

Nenhuma das duas era bug de fixture.

**A guarda do token estava na camada errada.** Eu a coloquei em
`src/apis/session/`, e dois testes de arquitetura reprovaram: `src/apis/**` não
pode tocar armazenamento do navegador (B10), e `_core` alcançando o interior de
outro módulo de API é bypass de camada (B13). As duas regras estavam certas.
O arquivo foi para `src/lib/auth/`, onde as preocupações de ambiente vivem —
como `src/lib/offline/`. **A regra não foi afrouxada para acomodar o arquivo.**

**A sessão lia o valor do token sem precisar dele.** `nativeSessionApi` chamava
`getNativeToken()` só para saber se havia sessão. Isso fazia
`P4-NATIVE-HTTP-BOUNDARY` reprovar — corretamente. A correção foi no código, não
no gate: a API passou a usar `hasNativeToken()`, presença em vez de valor. Agora
"só o cliente HTTP lê o token" é literalmente verdade e verificável.

## 9. Base44 — redução real

| Eixo | Antes | Depois |
|---|---|---|
| `authRefs` | 10 | **5** |
| Todos os outros | — | **inalterados** |

`4/1/38/10/1/1/1/1/38/1` → `4/1/38/**5**/1/1/1/1/38/1`.

A redução veio de código que **realmente** deixou de ser necessário:
`redirectToLogin` (autenticaria no provider errado) e `logout` (não deve ser
chamado — ver §4 e D-PROD-24 item G). Nenhum eixo cresceu, nada foi mascarado,
nenhum schema Base44 foi excluído, o SDK continua instalado e o plugin do Vite
intacto. Baseline atualizado com `--update`, o que a missão autoriza nesse caso.

## 10. Testes

| Suite | Antes | Depois |
|---|---|---|
| `test:gates` | 428 | **452** (+24, `native-api`) |
| `test:backend` | 41 | **60** (+19, CORS e bootstrap) |
| `test:smoke` | 495 | **521** (+26, sessão nativa e casca) |
| **Total** | 964 | **1.033** |

Casos novos: `NAT-01..24`, `P4B-01..10` com sub-casos, `P4T-01..17` com
sub-casos, mais `AUTH5/AUTH9..AUTH13` reescritos.

**Uma corrida real encontrada e corrigida.** Com dois arquivos de teste de
backend, o runner do Node paraleliza por arquivo — e o `limparBanco` de um
apagava as linhas que o outro acabara de criar. O sintoma era erro de Prisma em
testes sem defeito nenhum, que passavam isolados: o pior tipo de vermelho,
porque parece bug de código e é corrida de infraestrutura. `run-backend-tests`
passou a fixar `--test-concurrency=1`.

## 11. Tipos

| Contrato | Antes | Depois |
|---|---|---|
| `src/` (catraca) | 2.319, teto 2.319 | **2.318, teto 2.318** |
| `backend/` | 0 | **0** |

O código novo introduziu **11 diagnósticos**, e nenhum foi silenciado — sem
`any`, sem `@ts-ignore`, sem `@ts-nocheck`. As três classes:

- **`unknown` do transporte.** `nativeRequest` devolve `unknown` de propósito.
  Corrigido com **type guards declarados** (`respostaDeLoginValida`,
  `contextoValido`, `ehObjeto`), que é o oposto de suprimir: o conhecimento que
  já existia em runtime passou a existir no tipo;
- **união de códigos perdida.** `Set.has(x) ? x : …` devolve `string` solto, e o
  contrato de `ApiError` deixaria de ser verificável. Trocado por um mapa
  código→código, que preserva o literal;
- **contexto de React sem tipo.** Isso fazia o `onClick` do logout no `Layout`
  virar `any` — dívida nascendo em código novo, exatamente o que a catraca
  existe para impedir. Corrigido declarando o contrato do contexto.

O total caiu **1** abaixo do teto, sem regressão de impressão digital, e o
baseline foi atualizado — o que a missão autoriza nesse caso.

## 12. Prisma — congelado

`backend/prisma/schema.prisma` **inalterado**. Zero migration nova. Cinco
models: `Cliente`, `Usuario`, `AuditLog`, `EntidadeCodigoSequencia`,
`RegistroAnexo`.

Nenhum model geográfico. `Setor`, `AreaPastagem`, `PontoReferencia`,
`PontoSuplementacao`, `LinhaGeografica`, `ConfiguracaoIcone` e
`MovimentacaoMapa` continuam só na Base44. `proximoNumeroSetor` e `numero_area`
não foram tocados — a correção do `MAX+1` é da P4.1.

## 13. Build

| | Base `672ea99` | P4.0 |
|---|---|---|
| Módulos | 3.941 | **3.945** |
| JS | 2.496,62 kB | **2.503,50 kB** |
| gzip | 669,03 kB | **671,09 kB** |
| CSS | 77,00 kB | **77,11 kB** |

**+6,88 kB (+0,28%)**, quatro módulos novos: transporte, guarda do token, sessão
nativa e formulário de login. O aviso de chunk acima de 500 kB continua ligado e
não foi silenciado; code splitting segue como DBT-06, para a P8.

## 14. `verify:all` — 19/19

```
  ETAPA                RESULTADO   EXIT    DURAÇÃO
  test:gates           PASS           0      64.2s
  governance-paths     PASS           0      177ms
  package-sync         PASS           0      200ms
  product-scope        PASS           0      648ms
  api-boundary         PASS           0       1.5s
  native-api           PASS           0      198ms   ← etapa nova
  source-closure       PASS           0      203ms
  import-integrity     PASS           0      213ms
  no-secrets           PASS           0      507ms
  base44               PASS           0      156ms
  modelobase1-pecuario PASS           0      154ms
  tenancy              PASS           0      180ms
  indices              PASS           0      171ms
  types                PASS           0      10.3s
  typecheck:backend    PASS           0       1.9s
  lint                 PASS           0       8.1s
  test:backend         PASS           0       9.5s
  test:smoke           PASS           0      11.5s
  build                PASS           0      10.6s

  Total: 120.3s

TODAS AS ETAPAS PASSARAM
```

Contra PostgreSQL 16 local e descartável. Nunca staging, Supabase, banco externo
ou banco do proprietário.

## 15. Preservação

| Invariante | Estado |
|---|---|
| `gate:api-boundary` | **0/0/0/0/0/0** |
| Registry do provider | **38** |
| `gate:base44` | `4/1/38/5/1/1/1/1/38/1` — só `authRefs` desceu |
| `gate:types` | **2.318, teto 2.318** |
| `typecheck:backend` | **0** |
| `lint` | **0** |
| `gate:modelobase1-pecuario` | verde, absoluto, não enfraquecido |
| Backend sem Base44 | **zero**, travado por `P3-TEN-BASE44` |
| Models Prisma | **5**, schema inalterado |

`base44/`, `config/mapa-manejo-scope.json` e `config/modelobase1-pecuario.json`
não foram tocados. Nenhuma rota nova, nenhuma página nova, `pages.config` e o
manifesto de escopo intactos.

## 16. Divergências declaradas

**Uma, e pequena.** A missão pedia `VITE_MAIKE_API_URL=http://localhost:3333`
no `.env.example`. O repositório tem regra anterior e mais estrita — ENV2 exige
que **nenhuma** `VITE_*` do arquivo carregue valor real, porque exemplo
preenchido é a forma mais comum de um valor de desenvolvimento acabar num build
de produção. A variável entrou com valor vazio e o `http://localhost:3333` ficou
no comentário logo acima: mesma informação, regra intacta.

## 17. Riscos e pendências

| Item | Estado |
|---|---|
| XSS lê o `sessionStorage` | **declarado**, não resolvido — P8 (D-PROD-24 F) |
| Refresh token, revogação, CSP | fora de escopo por decisão — P8 |
| Gestão de usuários (CRUD, RBAC) | P6; o bootstrap **não** é CRUD |
| OWNER-SECURITY-01 | aberto, ação do proprietário |
| Base44 como provider de dados | até a P7 |
| `numero_lote`/`numero_setor` por `MAX+1` | P4.1 (DBT-26) |
| Bundle sem code splitting | P8 (DBT-06) |
| `npm audit` | P8 |
| Deprecation `disableRequestLogging` | não tocada de propósito, fora do escopo |

## 18. Estado

**P4.0 implementada, em PR draft. P4 em execução. P4.1 (Setor) não iniciada.
P5, P6, P7 e P8 congeladas.**
