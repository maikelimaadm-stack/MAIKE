# Copie para .env.local e preencha com valores reais. NUNCA versione .env*.

# Chave da API do Google Maps usada pelo Mapa Geral e pelo Cadastro do Mapa.
# Sem esta variável o mapa exibe mensagem de configuração ausente (não fica em branco).
VITE_GOOGLE_MAPS_API_KEY=
